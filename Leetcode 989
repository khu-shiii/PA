

class Solution {
    public List<Integer> addToArrayForm(int[] num, int k) {
        List<Integer> res = new ArrayList<>();
        int i = num.length - 1;
        
      
        while (i >= 0 || k > 0) {
            if (i >= 0) {
                k += num[i];
                i--;
            }
            
            // k % 10 gives the digit for the current position
            res.add(k % 10);
            
            // k /= 10 carries the rest over to the next column
            k /= 10;
        }
        
        //we need to reverse the arraylist
        Collections.reverse(res);
        return res;
    }
}
